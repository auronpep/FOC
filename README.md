# FOC

Fresh local OpenClaw install rooted at `C:\FOC`.

## Local command

Use:

```powershell
C:\FOC\bin\openclaw.ps1 --version
C:\FOC\bin\openclaw.ps1 status
```

The wrapper pins state and config to `C:\FOC\.openclaw` and the agent workspace to `C:\FOC\Workspace`.
